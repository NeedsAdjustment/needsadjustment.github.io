<div class="snaps-overlay" role="dialog" aria-label="Photo gallery" data-lenis-prevent>
  <masonry-grid-lanes min-column-width="200" gap="12">
  </masonry-grid-lanes>
</div>

<button class="snaps-close" aria-label="Close gallery">
  <i class="ph-bold ph-x"></i>
</button>

<div class="snaps-lightbox">
  <img class="snaps-lightbox-img" alt="">
  <i class="snaps-spinner"></i>
  <button class="snaps-prev" aria-label="Previous">
    <i class="ph-bold ph-caret-left"></i>
  </button>
  <button class="snaps-next" aria-label="Next">
    <i class="ph-bold ph-caret-right"></i>
  </button>
</div>

<script>
  import '../scripts/snaps.js'
</script>

<style is:global>
  .snaps-overlay {
    scrollbar-width: none;
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: none;
    flex-direction: column;
    background: rgba(0,0,0,0.85);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    opacity: 0;
    overflow-y: auto;
    transition: opacity 0.3s ease;
  }

  .snaps-close {
    position: fixed;
    top: 0.5rem;
    right: 0.25rem;
    background: none;
    border: none;
    color:  var(--ui-border);
    font-size: 1.8rem;
    cursor: pointer;
    z-index: 10002;
    line-height: 1;
    transition: color 0.2s;
  }
  .snaps-close:hover {
    color: var(--text-color);
  }

  masonry-grid-lanes {
    width: auto;
    box-sizing: border-box;
    margin: 2rem;
    position: relative;
  }

  .snaps-lightbox {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 10001;
    align-items: center;
    justify-content: center;
    background: rgba(0,0,0,0.92);
    cursor: pointer;
  }

  .snaps-lightbox-img {
    max-width: calc(100vw - 5rem);
    max-height: calc(100dvh - 5rem);
    object-fit: contain;
    cursor: default;
    user-select: none;
    -webkit-user-drag: none;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .snaps-spinner {
    display: none;
    position: fixed;
    z-index: 10002;
    pointer-events: none;
    font-family: 'Teranoptia';
    font-size: 4rem;
    line-height: 1;
    animation: snaps-spin 0.8s linear infinite;
  }

  .snaps-prev,
  .snaps-next {
    display: none;
    position: fixed;
    top: 50%;
    transform: translateY(-50%);
    border: none;
    background: none;
    color:  var(--ui-border);
    font-size: 2rem;
    cursor: pointer;
    border-radius: 8px;
    z-index: 10002;
    transition: color 0.2s;
    line-height: 1;
  }
  .snaps-prev { left: 0; }
  .snaps-next { right: 0; }
  .snaps-prev:hover,
  .snaps-next:hover { color: var(--text-color); }

  /* Thumbnail figures rendered by JS */
  .snaps-figure {
    margin: 0;
    border-radius: 6px;
    overflow: hidden;
    cursor: pointer;
    position: relative;
    background: rgba(255,255,255,0.05);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .snaps-figure:hover {
    transform: scale(1.03);
    box-shadow: 0 4px 20px rgba(0,0,0,0.4);
  }
  .snaps-figure img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  @keyframes snaps-spin {
    to { transform: rotate(360deg); }
  }
</style>
