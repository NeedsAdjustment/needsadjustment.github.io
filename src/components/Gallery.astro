<div class="snaps-overlay" role="dialog" aria-label="Photo gallery" data-lenis-prevent transition:persist="snaps-overlay">
  <div class="snaps-shadow-top"></div>
  <masonry-grid-lanes min-column-width=175 gap=16>
  </masonry-grid-lanes>
  <div class="snaps-shadow-bottom"></div>
</div>

<div class="snaps-lightbox" transition:persist="snaps-lightbox">
  <img class="snaps-lightbox-img" alt="">
  <i class="snaps-spinner"></i>
  <button class="snaps-prev" aria-label="Previous">
    <i class="ph-bold ph-caret-left"></i>
  </button>
  <button class="snaps-next" aria-label="Next">
    <i class="ph-bold ph-caret-right"></i>
  </button>
</div>

<script>
  import '../scripts/snaps.js'
</script>

<style is:global>
  .snaps-overlay {
    scrollbar-width: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100dvh;
    z-index: 5;
    display: none;
    flex-direction: column;
    background: var(--background-color);
    opacity: 0;
    overflow-y: auto;
    transition: opacity 0.3s ease;
  }

  .snaps-shadow-top,
  .snaps-shadow-bottom {
    position: sticky;
    left: 0;
    width: 100%;
    pointer-events: none;
    z-index: 1;
    flex-shrink: 0;
  }
  .snaps-shadow-top {
    top: 0;
    height: calc(var(--header-height) + 1rem);
    background: linear-gradient(to bottom, var(--base-color), transparent);
  }
  .snaps-shadow-bottom {
    bottom: 0;
    height: var(--header-height);
    background: linear-gradient(to top, var(--base-color), transparent);
  }

  masonry-grid-lanes {
    display: block;
    box-sizing: border-box;
    margin: 12px;
    position: relative;
  }

  .snaps-lightbox {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 10001;
    align-items: center;
    justify-content: center;
    background: rgba(0,0,0,0.92);
    cursor: pointer;
  }

  .snaps-lightbox-img {
    max-width: calc(100vw - 5rem);
    max-height: calc(100dvh - 5rem);
    object-fit: contain;
    cursor: default;
    user-select: none;
    -webkit-user-drag: none;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .snaps-spinner {
    display: none;
    position: fixed;
    z-index: 10002;
    pointer-events: none;
    font-family: 'Teranoptia';
    font-size: 4rem;
    line-height: 1;
    animation: snaps-spin 0.8s linear infinite;
  }

  .snaps-prev,
  .snaps-next {
    display: none;
    position: fixed;
    top: 50%;
    transform: translateY(-50%);
    border: none;
    background: none;
    color:  var(--ui-border);
    font-size: 2rem;
    cursor: pointer;
    border-radius: 8px;
    z-index: 10002;
    transition: color 0.2s;
    line-height: 1;
  }
  .snaps-prev { left: 0; }
  .snaps-next { right: 0; }
  .snaps-prev:hover,
  .snaps-next:hover { color: var(--text-color); }

  /* Thumbnail figures rendered by JS */
  .snaps-figure {
    margin: 0;
    border-radius: 1rem;
    overflow: hidden;
    cursor: pointer;
    position: relative;
    transition: transform 0.2s ease;
  }
  .snaps-figure:hover {
    transform: scale(1.02);
  }
  .snaps-figure img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  @keyframes snaps-spin {
    to { transform: rotate(360deg); }
  }
</style>
